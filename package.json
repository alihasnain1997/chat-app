{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "kill -9 $(lsof -t -i:3000) ;node src/index.js",
    "dev": "kill -9 $(lsof -t -i:3000) ;nodemon src/index.js",
    "clear": "kill -9 $(lsof -t -i:3000)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bad-words": "^3.0.4",
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
